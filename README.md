## How to run

```
Download and extract the zip
Go to project folder and launch the command prompt
In command prompt, run 'yarn install', then 'yarn start'
Application will be launched in the browser, else go to http://localhost:3000
```
